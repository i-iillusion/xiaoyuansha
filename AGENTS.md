# 开发约束与交接入口

本仓库是 Godot《校园杀》规则原型。不要凭模型记忆、旧聊天编号或既有代码发明玩法。

## 每次开始工作

1. 读取 `Docs/Agent/开发交接.md`、`Docs/HumanAgent/开发执行计划.md`、`Docs/HumanAgent/现行裁决索引.md`。
2. 检查分支、工作区、远端差异；保留他人和上轮未提交修改，不自动 stash/reset/覆盖。根目录用户 PDF 不纳入提交。
3. 选定计划中的一个任务 ID，读取对应手册原文、裁决原例及调用链，然后说明本批范围。
4. 规则优先级：负责人最新明确裁决 > 已确认采用的知识手册 > 已获授权且不冲突的经典基础规则。测试、代码注释、AI 策略和计划都不是规则来源。
5. B0已由负责人明确关闭：新版规则尚未完善，暂时不要作为参考。不得使用`origin/docs/knowledge-book`推导规则、填补空白或合入正文。仅当负责人以后明确改令才重新评估；历史差异表不是开发依据。

## 实施与验收

- 严格按执行计划依赖推进；问题只阻塞依赖它的子项。不得跳到复杂扩将来掩盖牌流转/时序基础缺口。
- 发现歧义：写出人数、座位、体力、牌区、技能、事件顺序、两种不同结果，登记固定问题 ID 并询问；保留“我也不清楚，需要与其他人商议”。不把未答复选项当默认答案。
- 默认手牌是任意牌；具体牌是已具体化后流转回手的牌。两类都要覆盖，不生成随机具体牌来代替普通摸牌，也不把具体牌重置为任意牌。
- 凡 `await` 选择后，复查动作、阶段、操作者、目标和牌实例。自愿放弃、强制选择、取消 UI、对象过期、游戏结束不是同一种结果。
- 费用、外部失牌效果、真实移动、使用/打出、伤害、失去体力、扣除体力须区分。烈火盾按 L-B 分类，不能用“来自技能”一刀切禁止。
- 预大习成功不死亡、不翻身份、不清手牌/装备/判定区；不得用先清牌再还牌实现。
- 本项目既有约定：未获用户明确要求，不手动运行游戏测试。编写回归并接入已有七组推送 CI；允许静态检查，不能写成运行验证。
- 小批提交到 dev；用户已授权阶段性推送。不得自动合 main，不强推，不提交凭据、个人临时材料或美术资产。
- 提交前逐文件审阅暂存 diff，检查没有夹带无关修改；推送后记录对应 SHA 的 CI URL/状态。失败先修同批，不降低测试门槛。
- 每批更新执行计划与 QA：规则状态 / 实现状态 / 验证状态分别写；没有完整入口回归不能宣布整个里程碑或初版完成。

## 结束时必须留下

本批任务 ID、改动范围、提交/远端状态、验证证据、未覆盖边界、待裁决编号、下一步精确入口。历史计划仅供追溯，不复活旧“待讨论”或旧清牌方案。
