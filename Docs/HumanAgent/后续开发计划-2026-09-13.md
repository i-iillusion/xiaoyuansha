# 后续开发计划（2026-09-13）

## 同步基线与工作约定

起点：dev `325ab28`（PR #9），[七组 Linux CI 已通过](https://github.com/i-iillusion/xiaoyuansha/actions/runs/34745307866)。ST-06/07 的基础乱斗、ST-09 的具体牌主动再使用、ST-30 的七组 CI 已进入远端开发线；旧文档“本地候选、待远端验证”为历史状态，不代表这些基础功能仍未合并。main 为 `01d8317`。

负责人授权先同步计划，再分里程碑实施、提交并推送 dev。本次不自动再合 main；不修改美术与根目录用户 PDF；不手动运行游戏测试，利用现有推送 CI。每批代码通过验证才记录为完成，不以计划或测试数量宣称初版完整。

## 实施顺序

| 批次 | 对应任务 | 交付边界与验收 |
| --- | --- | --- |
| A1 统一用牌支付 | ST-09/10 | hand 与 determined_cards 共用类型预查、主动支付、闪/杀响应、无懈/舍己和桃酒求救；保留原实例、具体类型和取消/离手保护。先保留两种存储，不同时重写全局牌数。 |
| A2 统一数量与弃牌 | ST-08/10/26 | 手中牌统一计数，清点空城/觉醒、手牌上限、技能费用、拆偷和弃牌阶段；不能只改 hand_size 而让旧 pop_back 路径空扣。完成具体选牌与原对象去向回归。 |
| B 装备实例生命周期 | ST-11 | 装备区保存真实资源，兼容旧类型查询；使用、替换、转移、归还及死亡不复制、不丢失，暗置占位不造牌。 |
| C 装备技能边界 | ST-12～14 | 苕抢占发生在装备成功前、普通坐骑不套同名唯一；烈火盾不能代付技能弃牌；可选技能拒绝后正确恢复原流程。 |
| D 基础 AI 闭环 | ST-19～22 | 先有限步回合推进，再基本牌/装备/锦囊与响应；共用 A/B 合法性和费用，不读隐藏身份，不等待仅玩家0能操作的按钮。已有基础救援保留并组合验证。 |
| E 现有武将与操作完整性 | ST-16/17/23～25 | 七将状态快照、回合/阶段次数、赠送阶段操作者和恢复点；拒绝、取消、超时均不产生半支付或重复技能。 |
| F 补齐里奥 | M4、QA-T10 | 先用牌事件和标记，再丑态/预习与锦囊替换；验证实体牌、目标约束、贤者复原、预大习嵌套。其他技能齐备才开放选将。未清楚的替换组合必须给具体例子裁定。 |
| G 模式与局末整合 | ST-07/18/27/28/31 | 乱斗与预大习、连环、延时锦囊组合；返回菜单、重开清理、详情弹窗释放；从选将到正确终局的完整对局验收材料。 |
| H 人数与身份扩展 | M5 | 动态 2～10 人布局；5/7/9 经典身份、6/8/10 奸雄模式配置及主公死亡声明/换身份；身份能力与乱斗隔离。按各模式验证后开放入口。 |
| I 轮次、空间与扩将 | M4～6 | 固定编号与动态位置分离、轮次边界、插入回合；先火龙和山，再按依赖继续其余武将。无人机/地雷所属安迪继续暂缓，不以此阻塞其他批次。 |

这是依赖顺序而非工期承诺。D 的只读决策设计可与 B/C 并行，F 可在 A 与所需事件入口稳定后提前，H 的布局可与其他批次并行；同时修改 GameManager 的分支须先协调，避免重复重构。

## 尚待裁定的局末案例（仅隔离相关组合）

两人乱斗 A、B 均1体力、均连环，无救援/保命。A 对 B 造成1点火伤，B先最终死亡。当前代码可以在外层连环传导前判 A 胜，并跳过后续传导；这是静态分析，未宣称已运行复现。

- [ ] B死亡立即判A获胜，停止传导。
- [ ] 完成传导，A也死亡后平局。
- [ ] 我也不清楚，需要与其他人商议。

负责人尚未答复。本批不改这个顺序；现有“全部已最终死亡判平局”的测试不等价于该过程已验收。答复同步 QA 后再补生产入口回归。

## 各批提交要求

进度：A1 已完成，代码提交 `e3fe6d9`，[七组自动 CI 通过](https://github.com/i-iillusion/xiaoyuansha/actions/runs/34746754370)。见 QA-T12；下一批为 A2，统一数量、弃牌/费用和拆偷，不是仅修改计数函数。计划本身先以 `df5b60a` 同步 dev。

先写最小回归案例，再改公共入口和调用者，检查引用与差异，分批提交推送 dev。自动 CI 失败先修本批，不合并 main、不放宽零失败门槛。记录提交、CI 链接、未覆盖边界和下一批入口；新增问题不得自动代填规则。
